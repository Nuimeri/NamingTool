{
  "name": "naming-tool",
  "version": "1.0.0",
  "description": "Google Apps Script project for AI Naming Assistant",
  "private": true,
  "scripts": {
    "build:test": "node build.js test",
    "build:deploy": "node build.js deploy",
    "push:test": "npm run build:test && clasp push",
    "push:deploy": "npm run build:deploy && clasp push"
  },
  "devDependencies": {
    "fs-extra": "^11.2.0"
  }
}